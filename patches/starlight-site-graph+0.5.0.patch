diff --git a/node_modules/starlight-site-graph/components/graph/preprocess-sitemap.ts b/node_modules/starlight-site-graph/components/graph/preprocess-sitemap.ts
index 454814d..23a23c0 100644
--- a/node_modules/starlight-site-graph/components/graph/preprocess-sitemap.ts
+++ b/node_modules/starlight-site-graph/components/graph/preprocess-sitemap.ts
@@ -1,5 +1,3 @@
-import micromatch from 'micromatch';
-
 import type { LinkData, NodeData } from './types';
 import type { Sitemap } from '../../config';
 import type { GraphComponent } from './graph-component';
@@ -14,6 +12,49 @@ export type GraphData = {
 	links: LinkData[];
 };
 
+/**
+ * Browser-safe glob matching.
+ *
+ * This module runs on the client, and `micromatch` is Node-only — it reads
+ * `process.platform` at import time, which throws "process is not defined" in a
+ * browser and kills the graph component before it can render. It was pulled in
+ * for the two glob tests below, so it is replaced with a small compiler for the
+ * subset of glob syntax the graph config actually uses (`**`, `*`, `?`).
+ */
+const globCache = new Map<string, RegExp>();
+
+function globToRegExp(glob: string): RegExp {
+	const cached = globCache.get(glob);
+	if (cached) return cached;
+
+	let source = '';
+	for (let i = 0; i < glob.length; i++) {
+		const char = glob[i]!;
+		if (char === '*') {
+			if (glob[i + 1] === '*') {
+				// `**` crosses separators; `**/` may also match zero directories.
+				if (glob[i + 2] === '/') {
+					source += '(?:.*\\/)?';
+					i += 2;
+				} else {
+					source += '.*';
+					i += 1;
+				}
+			} else {
+				source += '[^/]*';
+			}
+		} else if (char === '?') {
+			source += '[^/]';
+		} else {
+			source += char.replace(/[.+^${}()|[\]\\]/g, '\\$&');
+		}
+	}
+
+	const re = new RegExp(`^${source}$`);
+	globCache.set(glob, re);
+	return re;
+}
+
 function firstMatchingPattern(
 	text: string,
 	patterns: string | string[],
@@ -21,7 +62,7 @@ function firstMatchingPattern(
 ): boolean | undefined {
 	const patternList = typeof patterns === 'string' ? [patterns] : patterns;
 	for (const pattern of patternList) {
-		if (micromatch.isMatch(text, pattern.startsWith('!') ? pattern.slice(1) : pattern)) {
+		if (globToRegExp(pattern.startsWith('!') ? pattern.slice(1) : pattern).test(text)) {
 			return !pattern.startsWith('!');
 		}
 	}
diff --git a/node_modules/starlight-site-graph/components/graph/renderer.ts b/node_modules/starlight-site-graph/components/graph/renderer.ts
index 2a7c42d..bda4730 100644
--- a/node_modules/starlight-site-graph/components/graph/renderer.ts
+++ b/node_modules/starlight-site-graph/components/graph/renderer.ts
@@ -39,7 +39,11 @@ export class GraphRenderer {
 		await this.app.init({
 			antialias: true,
 			backgroundAlpha: 0,
-			resolution: Object.keys(this.context.sitemap).length > 5000 ? 2 : 4,
+			// Resolution 4 allocates a backing store 16x the CSS area, which puts a
+			// content-width graph into the multi-megapixel range and fails to draw
+			// (blank canvas) on software renderers and low-end GPUs. 2 is already
+			// beyond retina density for this content and keeps labels crisp.
+			resolution: Math.min(2, Math.max(1, globalThis.devicePixelRatio || 1) * 2),
 			resizeTo: this.container,
 		} as PIXI.ApplicationOptions);
 		this.container.appendChild(this.app.canvas);
diff --git a/node_modules/starlight-site-graph/components/graph/simulator.ts b/node_modules/starlight-site-graph/components/graph/simulator.ts
index 79cdefa..a0b9944 100644
--- a/node_modules/starlight-site-graph/components/graph/simulator.ts
+++ b/node_modules/starlight-site-graph/components/graph/simulator.ts
@@ -311,7 +311,30 @@ export class GraphSimulator {
 		let x;
 		let y;
 
-		if (this.currentNode) {
+		// Centre the graph's bounding box rather than the current page's node.
+		// Pinning the current node to the middle leaves the cluster leaning
+		// whenever that node is not itself central — measured at 48px of lean on a
+		// 608px box for a typical pillar page, with uneven margins either side.
+		// The current node keeps its own highlight, so "you are here" is not lost.
+		const bounds = this.nodes.length ? this.nodes.reduce(
+			(acc, n) => {
+				if (!Number.isFinite(n.x) || !Number.isFinite(n.y)) return acc;
+				acc.minX = Math.min(acc.minX, n.x!);
+				acc.maxX = Math.max(acc.maxX, n.x!);
+				acc.minY = Math.min(acc.minY, n.y!);
+				acc.maxY = Math.max(acc.maxY, n.y!);
+				acc.ok = true;
+				return acc;
+			},
+			{ minX: Infinity, maxX: -Infinity, minY: Infinity, maxY: -Infinity, ok: false },
+		) : { ok: false } as { minX: number; maxX: number; minY: number; maxY: number; ok: boolean };
+
+		if (bounds.ok) {
+			const centreX = (bounds.minX + bounds.maxX) / 2;
+			const centreY = (bounds.minY + bounds.maxY) / 2;
+			x = this.container.clientWidth / (2 * this.scale) - this.scale * centreX;
+			y = this.container.clientHeight / (2 * this.scale) - this.scale * centreY;
+		} else if (this.currentNode) {
 			x = this.container.clientWidth / (2 * this.scale) - this.scale * this.currentNode.x!;
 			y = this.container.clientHeight / (2 * this.scale) - this.scale * this.currentNode.y!;
 		} else {
diff --git a/node_modules/starlight-site-graph/config/index.ts b/node_modules/starlight-site-graph/config/index.ts
index 8a38bbf..5c59fc0 100644
--- a/node_modules/starlight-site-graph/config/index.ts
+++ b/node_modules/starlight-site-graph/config/index.ts
@@ -2,7 +2,7 @@ import { AstroError } from 'astro/errors';
 import { type StarlightSiteGraphConfig, starlightSiteGraphConfig, starlightSiteGraphConfigSchema } from './base';
 
 function isObject(item: any): boolean {
-	return (item && typeof item === 'object' && !Array.isArray(item));
+	return (item && typeof item === 'object' && !Array.isArray(item) && !(item instanceof Map) && !(item instanceof Set));
 }
 
 /**
diff --git a/node_modules/starlight-site-graph/sitemap/build.ts b/node_modules/starlight-site-graph/sitemap/build.ts
index 2957000..7689a63 100644
--- a/node_modules/starlight-site-graph/sitemap/build.ts
+++ b/node_modules/starlight-site-graph/sitemap/build.ts
@@ -348,7 +348,13 @@ export class SiteMapBuilder {
 			} else if (this.basePath !== '' && !trimSlashes(link).startsWith(this.basePath)) {
 				link = path.join(this.basePath, link);
 			}
-			link = slugifyPath(onlyTrailingSlash(link.split('#')[0]!, this.addTrailingSlash).replace(/\\/g, '/'));
+			link = onlyTrailingSlash(link.split('#')[0]!, this.addTrailingSlash).replace(/\\/g, '/');
+			// Keep the base path verbatim here too — see `getLinkPath` above.
+			if (this.basePath !== '' && (link === this.basePath || link.startsWith(this.basePath + '/'))) {
+				link = this.basePath + slugifyPath(link.slice(this.basePath.length));
+			} else {
+				link = slugifyPath(link);
+			}
 			if (link !== current) {
 				links.add(link);
 				return link;
@@ -377,10 +383,12 @@ export class SiteMapBuilder {
 			.slice(0, -path.extname(filePath).length || undefined);
 
 		// Ensure that the slashes are correct and honor the base option
-		relative_path = basePath === '' ? stripLeadingSlash(relative_path) : path.join(basePath, relative_path).replace(/\\/g, '/');
-
-		// Slugify the path, keeping slashes
-		relative_path = slugifyPath(relative_path);
+		// Slugify only the content-relative portion, then prepend the base path
+		// verbatim. Slugifying the base as well mangles any base containing a
+		// dot ("/paulmarinos.com" -> "paulmarinoscom"), which then never matches
+		// Astro's BASE_URL or the link targets resolved in `resolveLink`.
+		relative_path = slugifyPath(stripLeadingSlash(relative_path));
+		if (basePath !== '') relative_path = path.join(basePath, relative_path).replace(/\\/g, '/');
 
 		// Remove index from the end of the path
 		return ensureTrailingSlash(resolveIndex(relative_path), this.addTrailingSlash) || '/';
